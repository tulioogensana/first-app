/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { ComidaService } from "../comida.service";
import { ComidaCreateInput } from "./ComidaCreateInput";
import { ComidaWhereInput } from "./ComidaWhereInput";
import { ComidaWhereUniqueInput } from "./ComidaWhereUniqueInput";
import { ComidaFindManyArgs } from "./ComidaFindManyArgs";
import { ComidaUpdateInput } from "./ComidaUpdateInput";
import { Comida } from "./Comida";
@swagger.ApiBearerAuth()
export class ComidaControllerBase {
  constructor(
    protected readonly service: ComidaService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "Comida",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: Comida })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: ComidaCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Comida> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "Comida",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"Comida"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        usuarioId: data.usuarioId
          ? {
              connect: data.usuarioId,
            }
          : undefined,
      },
      select: {
        calorias: true,
        celiacos: true,
        costo: true,
        createdAt: true,
        foto: true,
        id: true,
        nombre: true,
        ultimoUso: true,
        updatedAt: true,

        usuarioId: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "Comida",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [Comida] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(ComidaFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Comida[]> {
    const args = plainToClass(ComidaFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Comida",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        calorias: true,
        celiacos: true,
        costo: true,
        createdAt: true,
        foto: true,
        id: true,
        nombre: true,
        ultimoUso: true,
        updatedAt: true,

        usuarioId: {
          select: {
            id: true,
          },
        },
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "Comida",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: Comida })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: ComidaWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Comida | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "Comida",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        calorias: true,
        celiacos: true,
        costo: true,
        createdAt: true,
        foto: true,
        id: true,
        nombre: true,
        ultimoUso: true,
        updatedAt: true,

        usuarioId: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "Comida",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: Comida })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: ComidaWhereUniqueInput,
    @common.Body()
    data: ComidaUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Comida | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "Comida",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"Comida"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          usuarioId: data.usuarioId
            ? {
                connect: data.usuarioId,
              }
            : undefined,
        },
        select: {
          calorias: true,
          celiacos: true,
          costo: true,
          createdAt: true,
          foto: true,
          id: true,
          nombre: true,
          ultimoUso: true,
          updatedAt: true,

          usuarioId: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "Comida",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: Comida })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: ComidaWhereUniqueInput
  ): Promise<Comida | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          calorias: true,
          celiacos: true,
          costo: true,
          createdAt: true,
          foto: true,
          id: true,
          nombre: true,
          ultimoUso: true,
          updatedAt: true,

          usuarioId: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id/comidas")
  @nestAccessControl.UseRoles({
    resource: "Comida",
    action: "read",
    possession: "any",
  })
  @ApiNestedQuery(ComidaFindManyArgs)
  async findManyComidas(
    @common.Req() request: Request,
    @common.Param() params: ComidaWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Comida[]> {
    const query = plainToClass(ComidaFindManyArgs, request.query);
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Comida",
    });
    const results = await this.service.findComidas(params.id, {
      ...query,
      select: {
        calorias: true,
        celiacos: true,
        costo: true,
        createdAt: true,
        foto: true,
        id: true,
        nombre: true,
        ultimoUso: true,
        updatedAt: true,

        usuarioId: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post("/:id/comidas")
  @nestAccessControl.UseRoles({
    resource: "Comida",
    action: "update",
    possession: "any",
  })
  async createComidas(
    @common.Param() params: ComidaWhereUniqueInput,
    @common.Body() body: ComidaWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      comidas: {
        connect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "Comida",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"Comida"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id/comidas")
  @nestAccessControl.UseRoles({
    resource: "Comida",
    action: "update",
    possession: "any",
  })
  async updateComidas(
    @common.Param() params: ComidaWhereUniqueInput,
    @common.Body() body: ComidaWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      comidas: {
        set: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "Comida",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"Comida"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id/comidas")
  @nestAccessControl.UseRoles({
    resource: "Comida",
    action: "update",
    possession: "any",
  })
  async deleteComidas(
    @common.Param() params: ComidaWhereUniqueInput,
    @common.Body() body: ComidaWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      comidas: {
        disconnect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "Comida",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"Comida"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
